description: 一个关于现场、音乐、照片和创作过程的个人档案。

identity:
  name: "WU 雾"
  label: "Harper Live Sessions"

# Restore project/release UI by setting this to true. Source URLs stay intact.
features:
  showWorks: false

home:
  projectsLimit: 3
  fieldNotesLimit: 4

# The table of contents printed on the cover spread. Order is the running
# order of the publication, not a navigation hierarchy.
cover:
  contents:
    - /projects
    - /field-notes
    - /now

social: []
